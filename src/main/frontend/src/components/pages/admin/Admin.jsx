import React from "react";
import { Tabs, Tab }from "react-bootstrap";

const Admin = (props) => {
    return (
            <div>
                admin
            </div>
            );
}

export default Admin;